import React from "react";


export default function Notification({,,...rest}){

    const [state, useState] = SetState([]);
    return (
        <div>
            Notification: {[props.value: string]}
        </div>
    );
}